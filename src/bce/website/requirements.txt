# Web framework
fastapi==0.104.1
uvicorn[standard]==0.24.0
python-multipart==0.0.6
jinja2==3.1.2
aiofiles==23.2.1

# Core dependencies (should already be installed in ReCEP environment)
# torch>=2.0.0
# numpy>=1.21.0
# scipy>=1.7.0
# h5py>=3.7.0
# tqdm>=4.64.0

# Bio dependencies (should already be installed in ReCEP environment)
# biotite>=0.39.0
# biopython>=1.81

# ESM-C dependencies (use the SDK version for ESM-C)
# Note: ESM SDK should be installed in ReCEP environment
# If not installed, use: pip install fair-esm[esmfold]

# Cloud path handling (should already be installed)
# cloudpathlib>=0.16.0

# Data processing (should already be installed)
# pandas>=1.5.0

# Visualization (should already be installed)
# matplotlib>=3.5.0

# Development dependencies (optional)
pytest>=7.0.0
pytest-asyncio>=0.21.0 